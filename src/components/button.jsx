import React from "react";

export const PryBtn = ({ children }) => {
  return (
    <>
      <button className="pry-btn">{children}</button>
    </>
  );
};

export const SecBtn = ({ children }) => {
  return (
    <>
      <button className="sec-btn">{children}</button>
    </>
  );
};
